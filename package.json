{
  "dependencies": {
    "aptos": "^1.21.0",
    "react-scripts": "^5.0.1"
  },
  "name": "aptos-bounty-board",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/my-app && npm install && npm run build"
  }
}
